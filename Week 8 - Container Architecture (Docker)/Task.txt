המשימה: ארכיטקטורת קונטיינרים (Docker)

המטרה: הבנה מדוע ואיך אורזים AI בתוך קונטיינרים, והפרדה בין סביבת פיתוח (Dev) לסביבת ייצור (Prod).

מה ללמוד:
- מדוע ואיך אורזים מערכות AI בתוך קונטיינרים
- הפרדה בין סביבת פיתוח (Dev) לסביבת ייצור (Prod)
- ניהול תשתית AI בסביבת Production

כלים: Docker, Docker Compose.
